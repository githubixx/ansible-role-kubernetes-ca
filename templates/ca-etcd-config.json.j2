{
  "signing": {
    "default": {
      "expiry": "{{ ca_etcd_expiry }}"
    },
    "profiles": {
      "etcd-server": {
         "usages": [
            "signing",
            "key encipherment",
            "server auth",
          ],
          "expiry": "{{ ca_etcd_expiry }}"
      }
    }
  }
}
